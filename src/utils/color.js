const kleur = require('kleur');

module.exports = {
  green: (s) => kleur.green(s),
  red: (s) => kleur.red(s),
  yellow: (s) => kleur.yellow(s),
  cyan: (s) => kleur.cyan(s),
  bold: (s) => kleur.bold(s)
};
